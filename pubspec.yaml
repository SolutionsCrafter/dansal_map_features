name: dansal_map_features
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_map: ^7.0.2
  flutter_map_location_marker: ^9.1.1
  flutter_map_marker_popup: ^7.0.0
  flutter_polyline_points: ^2.1.0
  geolocator: ^13.0.2
  latlong2: ^0.9.1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
